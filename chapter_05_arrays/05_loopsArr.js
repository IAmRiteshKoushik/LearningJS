// This is actually a function disguised as a loop
const a = [11, 22, 33]

// (element, index(optional), array(optional)) 
a.forEach((element, index) => {
    console.log(index, element);
});

// For more details, check w3schools.com

