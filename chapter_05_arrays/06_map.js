let arr = [45, 23, 21]

// Map returns a new array (basically mapping values to array indexes)
let a = arr.map((value) => {
    return value + 1;
})

console.log(a)
// forEach method does not return a new array

