// Reduce function in JavaScript
const arr = [1, 2, 3, 4, 5, 6, 7]
let arr2 = arr.reduce((h1, h2) => {
    return h1 + h2;
})
console.log(arr2)

// Reduce function is used to take in values
// from an array, compute them and then 
// store them in a variable. 


