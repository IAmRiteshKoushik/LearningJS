let name = 'Harry'
console.log(name);

// There is an issue with indexing if you do not 
// add semi-colons in javascript
// This issue does not happen in repl.it but 
// does happen when you try to run in local 
// machine (or) node.js
console.log(name[0]);
console.log(name[1]);
console.log(name[2]);
console.log(name[3]);
console.log(name[4]);

// Printing the final length
console.log(name.length);